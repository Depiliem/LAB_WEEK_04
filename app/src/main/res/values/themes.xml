<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema utama aplikasi -->
    <style name="Theme.LAB_WEEK_04" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Tambahkan warna/atribut custom kalau perlu -->
        <!-- <item name="colorPrimary">@color/my_primary</item> -->
    </style>

    <!-- Tema khusus kalau mau tanpa ActionBar (opsional) -->
    <style name="Theme.NavigationDrawer.NoActionBar" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.NavigationDrawer.AppBarOverlay" parent="ThemeOverlay.Material3.Dark.ActionBar" />
    <style name="Theme.NavigationDrawer.PopupOverlay" parent="ThemeOverlay.Material3.Light" />
</resources>
